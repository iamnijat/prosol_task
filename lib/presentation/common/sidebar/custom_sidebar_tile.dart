import 'package:flutter/material.dart';

class CustomSidebarTile extends StatelessWidget {
  const CustomSidebarTile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
